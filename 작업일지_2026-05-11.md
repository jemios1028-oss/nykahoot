# 작업일지 — 2026-05-11

## 추가 기능

### 관리자 실시간 답변 현황
- `question_active` 단계에서 현재 문제에 몇 명이 답변했는지 실시간 표시
- 진행률 바 + `N / 전체명` 형태로 표시
- 2초 폴링 방식 (Supabase Realtime 없이 동작)
- phase가 바뀌거나 문제가 바뀌면 카운트 자동 리셋
- 수정 파일: `src/pages/admin/AdminPage.jsx`

---

## 버그 수정 — Supabase 401 오류

### 원인 1: anon 롤 DML 권한 없음
`schema.sql`에 GRANT 구문이 없어서 anon 롤이 SELECT만 가능하고 INSERT/UPDATE/DELETE 불가.

**해결:** Supabase SQL Editor에서 아래 실행
```sql
grant select, insert, update, delete on public.students to anon;
grant select, insert, update, delete on public.answers to anon;
grant select, insert, update, delete on public.questions to anon;
grant select, update on public.game_state to anon;
grant usage, select on sequence public.questions_id_seq to anon;
grant usage, select on sequence public.answers_id_seq to anon;
```

### 원인 2: RLS 활성화 상태
`schema.sql`의 `disable row level security`가 적용되지 않은 상태.

**해결:** Supabase SQL Editor에서 아래 실행
```sql
alter table public.students disable row level security;
alter table public.answers disable row level security;
alter table public.questions disable row level security;
alter table public.game_state disable row level security;
```

### 원인 3: Storage 버킷 미설정
이미지 업로드용 `question-images` 버킷이 없어서 업로드 실패.

**해결:** Supabase SQL Editor에서 아래 실행
```sql
insert into storage.buckets (id, name, public)
values ('question-images', 'question-images', true)
on conflict (id) do nothing;

create policy "anon upload" on storage.objects
for insert to anon
with check (bucket_id = 'question-images');

create policy "anon read" on storage.objects
for select to anon
using (bucket_id = 'question-images');
```

### 참고: Supabase 신형 API 키
- Supabase가 새 키 형식(`sb_publishable_...`)으로 마이그레이션 중
- 현재 supabase-js와는 기존 JWT 형식(`eyJ...`) 레거시 키를 사용해야 함
- Settings → API → Legacy API keys 섹션에서 확인

---

## 현재 완성 상태

| 기능 | 상태 |
|------|------|
| 학생 참여 (학년/반/닉네임 입력) | ✅ |
| 대기실 실시간 참가자 수 | ✅ |
| 객관식 문제 풀기 | ✅ |
| 주관식 문제 풀기 | ✅ |
| 타이머 + 자동 순위 전환 | ✅ |
| 반별 순위 (막대 그래프 + 메달) | ✅ |
| 최종 순위 화면 | ✅ |
| 관리자 로그인 | ✅ |
| 문제 추가/삭제 (이미지 포함) | ✅ |
| 퀴즈 시작/다음 문제/초기화 | ✅ |
| 관리자 실시간 답변 현황 | ✅ |
